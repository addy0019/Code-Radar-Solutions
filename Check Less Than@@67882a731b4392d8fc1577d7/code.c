#include <stdio.h>

int main() {
      int num1, num2;    
    scanf("%d %d", &num1, &num2);
    if (num1 < num2) {
        printf(" True %d.\n");
    } else {
        printf(" False %d.\n");
    }

    return 0;
}
